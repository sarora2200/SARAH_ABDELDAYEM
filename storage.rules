rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Everything private by default; signed URLs bypass rules.
      allow read, write: if false;
    }
  }
}

